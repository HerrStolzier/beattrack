# Cross-Model-Review (commit-1ba55c5)

- Zeitpunkt: 2026-07-31T08:40:24
- Repo: beattrack
- Backend: /Users/ten.december/.npm-global/bin/codex
- Exit: 0

---

Die bisherigen Verhaltensprüfungen wurden weitgehend sinnvoll auf Postgres angepasst. Zwei neue Mocks prüfen jedoch nicht, ob die Datenbankoperation wirklich ausgeführt und auf die richtige Tabelle sowie Job-ID begrenzt wird. Der Testlauf konnte in der schreibgeschützten Umgebung nicht starten, weil kein temporäres Verzeichnis verfügbar ist.

Full review comments:

- [P2] Assert that the status update is actually persisted — /Users/ten.december/claude-projects/beattrack/apps/api/tests/test_analyze.py:76-79
  Wenn ein späterer Fehler `.execute()` entfernt oder auf eine falsche Tabelle schreibt, bleibt dieser Test grün: Er prüft nur den vorbereiteten Update-Inhalt. Dann würden Worker-Statusänderungen nie in Postgres gespeichert, obwohl die Tests bestehen. Prüfe zusätzlich den Aufruf von `table("analysis_jobs")` und `execute()`.

- [P2] Assert that job lookups are scoped to the requested ID — /Users/ten.december/claude-projects/beattrack/apps/api/tests/test_analyze.py:108-113
  Wenn der `eq("id", job_id)`-Filter später entfällt oder sich ändert, liefert `get_job` bei vorhandenen Daten möglicherweise den Job eines anderen Nutzers. Dieser Test bleibt trotzdem grün, weil sein Mock unabhängig vom angefragten Job immer eine leere Antwort liefert. Prüfe den Tabellen- und `eq`-Aufruf explizit.
OpenAI Codex v0.144.4
--------
workdir: /Users/ten.december/claude-projects/beattrack
model: gpt-5.6-terra
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 019fb6e5-cb96-7913-b71b-f358696dc715
--------
user
Reviewe die Aenderungen des Commits 1ba55c5. Zusaetzlicher Fokus: Fokus: Decken die angepassten Tests das gleiche Verhalten ab wie vorher? Wurde Testabdeckung verloren?
exec
/bin/zsh -lc 'cat /Users/ten.december/.agents/skills/check/SKILL.md && git status --short && git show --stat --oneline --decorate --no-renames 1ba55c5 && git diff 1ba55c5''^ 1ba55c5 --' in /Users/ten.december/claude-projects/beattrack
 succeeded in 5060ms:
2026-07-31 08:39:06.893 xcodebuild[83168:2567417]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:07.010 xcodebuild[83168:2567416] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-ZUThVjUQ' (errno=Operation not permitted)
2026-07-31 08:39:07.494 xcodebuild[83171:2567431]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:07.611 xcodebuild[83171:2567430] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
?? .agents/
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-rLiEsyQ9' (errno=Operation not permitted)
2026-07-31 08:39:08.609 xcodebuild[83176:2567450]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:08.724 xcodebuild[83176:2567449] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-0zoqdod0' (errno=Operation not permitted)
2026-07-31 08:39:09.175 xcodebuild[83178:2567465]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:09.293 xcodebuild[83178:2567464] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
1ba55c5 Repair analyze tests after job status moved to Postgres
 apps/api/tests/test_analyze.py | 106 +++++++++++++++++++++++++++++------------
 1 file changed, 76 insertions(+), 30 deletions(-)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-Cs3ItJNo' (errno=Operation not permitted)
2026-07-31 08:39:10.285 xcodebuild[83182:2567490]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:10.402 xcodebuild[83182:2567489] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-bYYZoM5e' (errno=Operation not permitted)
2026-07-31 08:39:10.841 xcodebuild[83184:2567502]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:10.956 xcodebuild[83184:2567501] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
diff --git a/apps/api/tests/test_analyze.py b/apps/api/tests/test_analyze.py
index f28f3ab..7fe3080 100644
--- a/apps/api/tests/test_analyze.py
+++ b/apps/api/tests/test_analyze.py
@@ -1,21 +1,21 @@
 """Tests for app/routes/analyze.py."""
 import io
-from unittest.mock import AsyncMock, MagicMock, patch
+from unittest.mock import MagicMock, patch
 
-import pytest
-from fastapi.testclient import TestClient
-
-from app.main import app
-from app.routes.analyze import _job_status, update_job_status
+from app.services import jobs
 
 
 # ---------------------------------------------------------------------------
 # Helpers
 # ---------------------------------------------------------------------------
 
-def _client():
-    c = TestClient(app)
-    return c
+def _supabase_mock():
+    """Supabase mock whose builder methods chain, exposing the update payload."""
+    sb = MagicMock()
+    builder = sb.table.return_value
+    for method in ("select", "eq", "in_", "insert", "update"):
+        getattr(builder, method).return_value = builder
+    return sb, builder
 
 
 # ---------------------------------------------------------------------------
@@ -30,17 +30,22 @@ def test_upload_no_file(client):
 
 # ---------------------------------------------------------------------------
 # Tests — unknown job ID
+#
+# Job status lives in Postgres since the split into api/worker containers, so
+# these routes hit the database. get_job is patched to keep tests offline.
 # ---------------------------------------------------------------------------
 
 def test_get_results_unknown_job(client):
     """GET /analyze/<unknown>/results should return 404."""
-    resp = client.get("/analyze/unknown-job-id/results")
+    with patch("app.services.jobs.get_job", return_value=None):
+        resp = client.get("/analyze/unknown-job-id/results")
     assert resp.status_code == 404
 
 
 def test_get_stream_unknown_job(client):
     """GET /analyze/<unknown>/stream should return 404."""
-    resp = client.get("/analyze/unknown-job-id/stream")
+    with patch("app.services.jobs.get_job", return_value=None):
+        resp = client.get("/analyze/unknown-job-id/stream")
     assert resp.status_code == 404
 
 
@@ -59,30 +64,71 @@ def test_upload_non_audio(client):
 
 
 # ---------------------------------------------------------------------------
-# Tests — update_job_status helper
+# Tests — jobs.update_job_status
 # ---------------------------------------------------------------------------
 
-def test_update_job_status():
-    """update_job_status() should set status and extra fields in _job_status."""
-    job_id = "test-job-status-001"
-    # Seed a job entry
-    _job_status[job_id] = {"status": "queued", "progress": 0.0}
+def test_update_job_status_writes_progress():
+    """update_job_status() should write status and progress to analysis_jobs."""
+    sb, builder = _supabase_mock()
+    with patch("app.services.jobs.get_supabase", return_value=sb):
+        jobs.update_job_status("test-job-status-001", "processing", progress=0.5)
+
+    row = builder.update.call_args[0][0]
+    assert row["status"] == "processing"
+    assert row["progress"] == 0.5
+    builder.eq.assert_called_with("id", "test-job-status-001")
+
 
-    update_job_status(job_id, "processing", progress=0.5)
+def test_update_job_status_completed_carries_result():
+    """A completed job should carry its result and a completion timestamp."""
+    sb, builder = _supabase_mock()
+    with patch("app.services.jobs.get_supabase", return_value=sb):
+        jobs.update_job_status(
+            "test-job-status-001", "completed", progress=1.0, result={"bpm": 128.0}
+        )
 
-    assert _job_status[job_id]["status"] == "processing"
-    assert _job_status[job_id]["progress"] == 0.5
+    row = builder.update.call_args[0][0]
+    assert row["status"] == "completed"
+    assert row["result"]["bpm"] == 128.0
+    assert row["completed_at"]
 
-    # Update to completed with result
-    update_job_status(job_id, "completed", progress=1.0, result={"bpm": 128.0})
-    assert _job_status[job_id]["status"] == "completed"
-    assert _job_status[job_id]["result"]["bpm"] == 128.0
 
-    # Cleanup
-    del _job_status[job_id]
+def test_update_job_status_survives_db_error():
+    """A failing status update must never abort the analysis."""
+    sb = MagicMock()
+    sb.table.side_effect = RuntimeError("database unreachable")
+    with patch("app.services.jobs.get_supabase", return_value=sb):
+        jobs.update_job_status("nonexistent-id", "processing")
 
 
-def test_update_job_status_unknown_id():
-    """update_job_status() on an unknown ID should silently do nothing."""
-    update_job_status("nonexistent-id", "processing")
-    assert "nonexistent-id" not in _job_status
+# ---------------------------------------------------------------------------
+# Tests — jobs.get_job
+# ---------------------------------------------------------------------------
+
+def test_get_job_unknown_returns_none():
+    """An unknown job ID should yield None, not an empty dict."""
+    sb, builder = _supabase_mock()
+    builder.execute.return_value = MagicMock(data=[])
+    with patch("app.services.jobs.get_supabase", return_value=sb):
+        assert jobs.get_job("nonexistent-id") is None
+
+
+def test_get_job_reports_stalled_job_as_failed():
+    """A processing job without progress past STALL_AFTER_SEC counts as failed."""
+    from datetime import datetime, timedelta, timezone
+
+    stalled_at = datetime.now(timezone.utc) - timedelta(seconds=jobs.STALL_AFTER_SEC + 60)
+    sb, builder = _supabase_mock()
+    builder.execute.return_value = MagicMock(data=[{
+        "id": "stalled-job",
+        "status": "processing",
+        "progress": 0.1,
+        "updated_at": stalled_at.isoformat(),
+        "audio_path": "/data/uploads/stalled-job.mp3",
+    }])
+
+    with patch("app.services.jobs.get_supabase", return_value=sb):
+        job = jobs.get_job("stalled-job")
+
+    assert job["status"] == "failed"
+    assert "stalled" in job["error"].lower()

exec
/bin/zsh -lc "printf '%s\\n' '--- current tests ---'; nl -ba apps/api/tests/test_analyze.py; printf '%s\\n' '--- parent tests ---'; git show 1ba55c5"'^:apps/api/tests/test_analyze.py | nl -ba; printf '"'%s\\n' '--- jobs ---'; nl -ba apps/api/app/services/jobs.py; printf '%s\\n' '--- routes ---'; nl -ba apps/api/app/routes/analyze.py; printf '%s\\n' '--- test config ---'; find . -name AGENTS.md -o -name pyproject.toml -o -name pytest.ini -o -name conftest.py | sort | xargs -r -n1 sh -c 'echo --- "'$0; sed -n "1,220p" "$0"'"'" in /Users/ten.december/claude-projects/beattrack
 succeeded in 2663ms:
2026-07-31 08:39:18.089 xcodebuild[83197:2567612]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:18.202 xcodebuild[83197:2567611] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-dHjmp25Y' (errno=Operation not permitted)
2026-07-31 08:39:18.636 xcodebuild[83199:2567622]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:18.749 xcodebuild[83199:2567621] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
     1	"""Tests for app/routes/analyze.py."""
     2	import io
     3	from unittest.mock import AsyncMock, MagicMock, patch
     4	
     5	import pytest
     6	from fastapi.testclient import TestClient
     7	
     8	from app.main import app
     9	from app.routes.analyze import _job_status, update_job_status
    10	
    11	
    12	# ---------------------------------------------------------------------------
    13	# Helpers
    14	# ---------------------------------------------------------------------------
    15	
    16	def _client():
    17	    c = TestClient(app)
    18	    return c
    19	
    20	
    21	# ---------------------------------------------------------------------------
    22	# Tests — no file upload
    23	# ---------------------------------------------------------------------------
    24	
    25	def test_upload_no_file(client):
    26	    """POST /analyze without a file should return 422 (validation error)."""
    27	    resp = client.post("/analyze")
    28	    assert resp.status_code == 422
    29	
    30	
    31	# ---------------------------------------------------------------------------
    32	# Tests — unknown job ID
    33	# ---------------------------------------------------------------------------
    34	
    35	def test_get_results_unknown_job(client):
    36	    """GET /analyze/<unknown>/results should return 404."""
    37	    resp = client.get("/analyze/unknown-job-id/results")
    38	    assert resp.status_code == 404
    39	
    40	
    41	def test_get_stream_unknown_job(client):
    42	    """GET /analyze/<unknown>/stream should return 404."""
    43	    resp = client.get("/analyze/unknown-job-id/stream")
    44	    assert resp.status_code == 404
    45	
    46	
    47	# ---------------------------------------------------------------------------
    48	# Tests — non-audio upload
    49	# ---------------------------------------------------------------------------
    50	
    51	def test_upload_non_audio(client):
    52	    """POST /analyze with a .txt file should be rejected with 400."""
    53	    fake_content = b"hello, this is a text file"
    54	    resp = client.post(
    55	        "/analyze",
    56	        files={"file": ("test.txt", io.BytesIO(fake_content), "text/plain")},
    57	    )
    58	    assert resp.status_code == 400
    59	
    60	
    61	# ---------------------------------------------------------------------------
    62	# Tests — update_job_status helper
    63	# ---------------------------------------------------------------------------
    64	
    65	def test_update_job_status():
    66	    """update_job_status() should set status and extra fields in _job_status."""
    67	    job_id = "test-job-status-001"
    68	    # Seed a job entry
    69	    _job_status[job_id] = {"status": "queued", "progress": 0.0}
    70	
    71	    update_job_status(job_id, "processing", progress=0.5)
    72	
    73	    assert _job_status[job_id]["status"] == "processing"
    74	    assert _job_status[job_id]["progress"] == 0.5
    75	
    76	    # Update to completed with result
    77	    update_job_status(job_id, "completed", progress=1.0, result={"bpm": 128.0})
    78	    assert _job_status[job_id]["status"] == "completed"
    79	    assert _job_status[job_id]["result"]["bpm"] == 128.0
    80	
    81	    # Cleanup
    82	    del _job_status[job_id]
    83	
    84	
    85	def test_update_job_status_unknown_id():
    86	    """update_job_status() on an unknown ID should silently do nothing."""
    87	    update_job_status("nonexistent-id", "processing")
    88	    assert "nonexistent-id" not in _job_status
--- jobs ---
     1	"""Job-Status in Postgres (Tabelle analysis_jobs) statt im Prozessspeicher.
     2	
     3	API und Worker laufen als getrennte Container. Ein Dictionary im Speicher ist
     4	dann pro Prozess privat — der Fortschritt des Workers wäre für die API
     5	unsichtbar. Die Tabelle analysis_jobs existierte bereits mit passendem Schema
     6	und wird hier erstmals verdrahtet (siehe infra-migration/docs/plan-audio-analyse.md).
     7	
     8	Alle Funktionen sind synchron (der Supabase-Client blockiert). In asynchronen
     9	Pfaden immer über asyncio.to_thread aufrufen.
    10	"""
    11	import logging
    12	from datetime import datetime, timezone
    13	from pathlib import Path
    14	
    15	from app.db import get_supabase
    16	
    17	logger = logging.getLogger(__name__)
    18	
    19	# Ein processing-Job ohne Statusupdate seit dieser Zeit gilt als abgestürzt.
    20	# Der Worker meldet Fortschritt bei 0.1/0.8/1.0 — Stillstand heißt Absturz.
    21	# Bewusst unterhalb des SSE-Abbruchs (900 s), damit der Nutzer die Meldung sieht.
    22	STALL_AFTER_SEC = 300
    23	
    24	# Notbremsen für den Upload-Ordner (gemeinsames Volume, wird nie pauschal geleert)
    25	ORPHAN_MAX_AGE_SEC = 24 * 3600
    26	VOLUME_CAP_BYTES = 5 * 1024**3
    27	
    28	
    29	def _now() -> str:
    30	    return datetime.now(timezone.utc).isoformat()
    31	
    32	
    33	def create_job(job_id: str, audio_path: str, duration_sec: float | None) -> None:
    34	    get_supabase().table("analysis_jobs").insert({
    35	        "id": job_id,
    36	        "status": "queued",
    37	        "progress": 0.0,
    38	        "audio_path": audio_path,
    39	        "duration_sec": duration_sec,
    40	    }).execute()
    41	
    42	
    43	def update_job_status(job_id: str, status: str, progress: float | None = None,
    44	                      result: dict | None = None, error: str | None = None) -> None:
    45	    """Statusupdate aus API oder Worker. Signatur kompatibel zum alten Wrapper."""
    46	    row: dict = {"status": status, "updated_at": _now()}
    47	    if progress is not None:
    48	        row["progress"] = progress
    49	    if result is not None:
    50	        row["result"] = result
    51	    if error is not None:
    52	        row["last_error"] = error
    53	    if status in ("completed", "failed"):
    54	        row["completed_at"] = _now()
    55	    try:
    56	        get_supabase().table("analysis_jobs").update(row).eq("id", job_id).execute()
    57	    except Exception as exc:  # Status-Updates dürfen die Analyse nie abbrechen
    58	        logger.warning("Job-Statusupdate fehlgeschlagen (%s -> %s): %s", job_id, status, exc)
    59	
    60	
    61	def get_job(job_id: str) -> dict | None:
    62	    """Liefert den Job im Format des alten _job_status-Eintrags, oder None.
    63	
    64	    Wendet die Hängend-Regel an: processing ohne Update seit STALL_AFTER_SEC
    65	    wird als failed gemeldet (nur in der Antwort — die Zeile bleibt unverändert,
    66	    ein erfolgreicher später eintreffender Worker-Update gewinnt).
    67	    """
    68	    res = get_supabase().table("analysis_jobs").select("*").eq("id", job_id).execute()
    69	    if not res.data:
    70	        return None
    71	    row = res.data[0]
    72	    status = row["status"]
    73	
    74	    if status == "processing" and row.get("updated_at"):
    75	        updated = datetime.fromisoformat(row["updated_at"].replace("Z", "+00:00"))
    76	        age = (datetime.now(timezone.utc) - updated).total_seconds()
    77	        if age > STALL_AFTER_SEC:
    78	            return {
    79	                "status": "failed",
    80	                "progress": row.get("progress", 0.0),
    81	                "error": "Analysis stalled (no progress for 5 minutes)",
    82	                "audio_path": row.get("audio_path"),
    83	            }
    84	
    85	    return {
    86	        "status": status,
    87	        "progress": row.get("progress", 0.0),
    88	        "result": row.get("result"),
    89	        "error": row.get("last_error"),
    90	        "audio_path": row.get("audio_path"),
    91	        "duration_sec": row.get("duration_sec"),
    92	    }
    93	
    94	
    95	def cleanup_temp_files(temp_dir: str) -> None:
    96	    """Räumt den gemeinsamen Upload-Ordner auf, ohne laufende Jobs zu gefährden.
    97	
    98	    Löscht nur: Dateien erledigter Jobs (completed/failed), verwaiste Dateien
    99	    ohne Job-Zeile älter als 24 h, und — als harte Obergrenze — die ältesten
   100	    Dateien ohne aktiven Job, bis das Volumen wieder unter 5 GB liegt.
   101	    Ein pauschales Leeren (früher rmtree beim Shutdown) gibt es bewusst nicht mehr.
   102	    """
   103	    root = Path(temp_dir)
   104	    if not root.exists():
   105	        return
   106	    files = [f for f in root.iterdir() if f.is_file()]
   107	    if not files:
   108	        return
   109	
   110	    ids = [f.stem for f in files]
   111	    active: set[str] = set()
   112	    finished: set[str] = set()
   113	    try:
   114	        res = get_supabase().table("analysis_jobs").select("id,status").in_("id", ids).execute()
   115	        for row in res.data or []:
   116	            (finished if row["status"] in ("completed", "failed") else active).add(str(row["id"]))
   117	    except Exception as exc:
   118	        logger.warning("Cleanup: Job-Abfrage fehlgeschlagen, lösche nur nach Alter: %s", exc)
   119	
   120	    import time
   121	    now = time.time()
   122	    for f in files:
   123	        try:
   124	            if f.stem in finished:
   125	                f.unlink(missing_ok=True)
   126	            elif f.stem not in active and now - f.stat().st_mtime > ORPHAN_MAX_AGE_SEC:
   127	                f.unlink(missing_ok=True)
   128	        except OSError as exc:
   129	            logger.warning("Cleanup: %s nicht löschbar: %s", f.name, exc)
   130	
   131	    remaining = sorted((f for f in root.iterdir() if f.is_file()), key=lambda f: f.stat().st_mtime)
   132	    total = sum(f.stat().st_size for f in remaining)
   133	    for f in remaining:
   134	        if total <= VOLUME_CAP_BYTES:
   135	            break
   136	        if f.stem in active:
   137	            continue
   138	        total -= f.stat().st_size
   139	        f.unlink(missing_ok=True)
   140	        logger.warning("Cleanup: Volumen-Obergrenze — %s gelöscht", f.name)
--- routes ---
     1	"""Audio analysis endpoints — upload, SSE streaming, results polling."""
     2	import asyncio
     3	import json
     4	import logging
     5	import os
     6	import tempfile
     7	import uuid
     8	from pathlib import Path
     9	
    10	from fastapi import APIRouter, HTTPException, Request, UploadFile
    11	from fastapi.responses import JSONResponse
    12	from pydantic import BaseModel
    13	from sse_starlette.sse import EventSourceResponse
    14	
    15	from app.limiter import limiter
    16	from app.services import jobs
    17	from app.services.validation import validate_upload, validate_audio
    18	
    19	logger = logging.getLogger(__name__)
    20	
    21	router = APIRouter(prefix="/analyze", tags=["analyze"])
    22	
    23	# Concurrency controls
    24	UPLOAD_SEMAPHORE = asyncio.Semaphore(3)  # Max 3 concurrent uploads
    25	SSE_LIMITER = asyncio.Semaphore(50)  # Max 50 SSE connections
    26	
    27	# Temp directory for uploads
    28	TEMP_DIR = os.environ.get("BEATTRACK_TEMP_DIR", tempfile.mkdtemp(prefix="beattrack_"))
    29	Path(TEMP_DIR).mkdir(parents=True, exist_ok=True)
    30	
    31	# Job-Zustand liegt in Postgres (app.services.jobs) — API und Worker sind
    32	# getrennte Container und teilen keinen Prozessspeicher mehr.
    33	
    34	
    35	class AnalyzeResponse(BaseModel):
    36	    job_id: str
    37	    status: str
    38	
    39	
    40	@router.post("", response_model=AnalyzeResponse)
    41	@limiter.limit("10/minute")
    42	async def upload_and_analyze(request: Request, file: UploadFile):
    43	    """Upload audio file for analysis.
    44	
    45	    Validates the file, saves to temp, enqueues analysis job.
    46	    Returns job_id for tracking via SSE or polling.
    47	    """
    48	    # Check capacity
    49	    if UPLOAD_SEMAPHORE._value == 0:
    50	        raise HTTPException(status_code=503, detail="Server at capacity. Please retry shortly.")
    51	
    52	    async with UPLOAD_SEMAPHORE:
    53	        # 1. Validate upload (MIME + size)
    54	        await validate_upload(file)
    55	
    56	        # 2. Save to temp file
    57	        job_id = str(uuid.uuid4())
    58	        ext = Path(file.filename or "audio.mp3").suffix or ".mp3"
    59	        temp_path = os.path.join(TEMP_DIR, f"{job_id}{ext}")
    60	
    61	        try:
    62	            content = await file.read()
    63	            with open(temp_path, "wb") as f:
    64	                f.write(content)
    65	        except Exception as exc:
    66	            logger.error("Failed to save upload: %s", exc)
    67	            raise HTTPException(status_code=500, detail="Failed to save uploaded file.")
    68	
    69	        # 3. Validate audio (ffprobe: format, duration) — sync call, run in executor
    70	        loop = asyncio.get_event_loop()
    71	        audio_info = await loop.run_in_executor(None, validate_audio, temp_path)
    72	
    73	        # 4. Track job status
    74	        await asyncio.to_thread(
    75	            jobs.create_job, job_id, temp_path, audio_info.get("duration_sec")
    76	        )
    77	
    78	        # 5. Enqueue analysis job
    79	        try:
    80	            from app.workers import app as procrastinate_app, analyze_audio
    81	            analyze_audio.defer(audio_path=temp_path, job_id=job_id)
    82	        except Exception as exc:
    83	            logger.error("Failed to enqueue job %s: %s", job_id, exc)
    84	            await asyncio.to_thread(jobs.update_job_status, job_id, "failed", error="enqueue failed")
    85	            Path(temp_path).unlink(missing_ok=True)
    86	            raise HTTPException(status_code=502, detail="Failed to start analysis.")
    87	
    88	        return AnalyzeResponse(job_id=job_id, status="queued")
    89	
    90	
    91	@router.get("/{job_id}/stream")
    92	async def stream_progress(job_id: str):
    93	    """SSE endpoint for real-time analysis progress.
    94	
    95	    Events:
    96	    - status: queued | processing | completed | failed
    97	    - progress: 0.0 - 1.0 (during processing)
    98	    - result: full analysis result (on completed)
    99	    - heartbeat: keepalive every 15s
   100	    """
   101	    if await asyncio.to_thread(jobs.get_job, job_id) is None:
   102	        raise HTTPException(status_code=404, detail="Job not found")
   103	
   104	    if SSE_LIMITER._value == 0:
   105	        raise HTTPException(status_code=503, detail="Too many active connections.")
   106	
   107	    async def event_generator():
   108	        async with SSE_LIMITER:
   109	            heartbeat_interval = 15
   110	            # 900 s statt 300: abgestimmt auf die Hängend-Erkennung (300 s) in
   111	            # services.jobs — eine langsame echte Analyse wird nicht abgeschnitten.
   112	            stale_timeout = 900
   113	            elapsed = 0.0
   114	
   115	            while elapsed < stale_timeout:
   116	                job = await asyncio.to_thread(jobs.get_job, job_id)
   117	                if not job:
   118	                    yield {"event": "error", "data": json.dumps({"detail": "Job not found"})}
   119	                    return
   120	
   121	                status = job["status"]
   122	
   123	                if status == "completed":
   124	                    yield {
   125	                        "event": "status",
   126	                        "data": json.dumps({
   127	                            "status": "completed",
   128	                            "progress": 1.0,
   129	                            "result": job.get("result"),
   130	                        }),
   131	                    }
   132	                    return
   133	
   134	                if status == "failed":
   135	                    yield {
   136	                        "event": "status",
   137	                        "data": json.dumps({
   138	                            "status": "failed",
   139	                            "error": job.get("error", "Analysis failed"),
   140	                        }),
   141	                    }
   142	                    return
   143	
   144	                # Send current status
   145	                yield {
   146	                    "event": "status",
   147	                    "data": json.dumps({
   148	                        "status": status,
   149	                        "progress": job.get("progress", 0.0),
   150	                    }),
   151	                }
   152	
   153	                # Wait before next update
   154	                await asyncio.sleep(heartbeat_interval)
   155	                elapsed += heartbeat_interval
   156	
   157	                # Send heartbeat
   158	                yield {"event": "heartbeat", "data": ""}
   159	
   160	            # Stale timeout reached
   161	            yield {"event": "error", "data": json.dumps({"detail": "Connection timed out"})}
   162	
   163	    return EventSourceResponse(event_generator())
   164	
   165	
   166	@router.get("/{job_id}/results")
   167	async def get_results(job_id: str):
   168	    """Polling fallback for analysis results.
   169	
   170	    Returns current job status. If completed, includes full results.
   171	    """
   172	    job = await asyncio.to_thread(jobs.get_job, job_id)
   173	    if not job:
   174	        raise HTTPException(status_code=404, detail="Job not found")
   175	
   176	    return JSONResponse(content={
   177	        "job_id": job_id,
   178	        "status": job["status"],
   179	        "progress": job.get("progress", 0.0),
   180	        "result": job.get("result") if job["status"] == "completed" else None,
   181	        "error": job.get("error") if job["status"] == "failed" else None,
   182	    })
   183	
--- test config ---
--- ./apps/api/.venv/lib/python3.12/site-packages/numpy/conftest.py
"""
Pytest configuration and fixtures for the Numpy test suite.
"""
import os
import sys
import tempfile
import warnings
from contextlib import contextmanager
from pathlib import Path

import hypothesis
import pytest

import numpy
from numpy._core._multiarray_tests import get_fpu_mode
from numpy.testing._private.utils import NOGIL_BUILD

try:
    from scipy_doctest.conftest import dt_config
    HAVE_SCPDT = True
except ModuleNotFoundError:
    HAVE_SCPDT = False

try:
    import pytest_run_parallel  # noqa: F401
    PARALLEL_RUN_AVAILABLE = True
except ModuleNotFoundError:
    PARALLEL_RUN_AVAILABLE = False

_old_fpu_mode = None
_collect_results = {}

# Use a known and persistent tmpdir for hypothesis' caches, which
# can be automatically cleared by the OS or user.
hypothesis.configuration.set_hypothesis_home_dir(
    os.path.join(tempfile.gettempdir(), ".hypothesis")
)

# We register two custom profiles for Numpy - for details see
# https://hypothesis.readthedocs.io/en/latest/settings.html
# The first is designed for our own CI runs; the latter also
# forces determinism and is designed for use via np.test()
hypothesis.settings.register_profile(
    name="numpy-profile", deadline=None, print_blob=True,
)
hypothesis.settings.register_profile(
    name="np.test() profile",
    deadline=None, print_blob=True, database=None, derandomize=True,
    suppress_health_check=list(hypothesis.HealthCheck),
)
# Note that the default profile is chosen based on the presence
# of pytest.ini, but can be overridden by passing the
# --hypothesis-profile=NAME argument to pytest.
_pytest_ini = os.path.join(os.path.dirname(__file__), "..", "pytest.ini")
hypothesis.settings.load_profile(
    "numpy-profile" if os.path.isfile(_pytest_ini) else "np.test() profile"
)

# The experimentalAPI is used in _umath_tests
os.environ["NUMPY_EXPERIMENTAL_DTYPE_API"] = "1"

def pytest_configure(config):
    config.addinivalue_line("markers",
        "valgrind_error: Tests that are known to error under valgrind.")
    config.addinivalue_line("markers",
        "leaks_references: Tests that are known to leak references.")
    config.addinivalue_line("markers",
        "slow: Tests that are very slow.")
    if not PARALLEL_RUN_AVAILABLE:
        config.addinivalue_line("markers",
            "parallel_threads(n): run the given test function in parallel "
            "using `n` threads.",
        )
        config.addinivalue_line("markers",
            "iterations(n): run the given test function `n` times in each thread",
        )
        config.addinivalue_line("markers",
            "thread_unsafe: mark the test function as single-threaded",
        )


def pytest_addoption(parser):
    parser.addoption("--available-memory", action="store", default=None,
                     help=("Set amount of memory available for running the "
                           "test suite. This can result to tests requiring "
                           "especially large amounts of memory to be skipped. "
                           "Equivalent to setting environment variable "
                           "NPY_AVAILABLE_MEM. Default: determined"
                           "automatically."))


gil_enabled_at_start = True
if NOGIL_BUILD:
    gil_enabled_at_start = sys._is_gil_enabled()


def pytest_sessionstart(session):
    available_mem = session.config.getoption('available_memory')
    if available_mem is not None:
        os.environ['NPY_AVAILABLE_MEM'] = available_mem


def pytest_terminal_summary(terminalreporter, exitstatus, config):
    if NOGIL_BUILD and not gil_enabled_at_start and sys._is_gil_enabled():
        tr = terminalreporter
        tr.ensure_newline()
        tr.section("GIL re-enabled", sep="=", red=True, bold=True)
        tr.line("The GIL was re-enabled at runtime during the tests.")
        tr.line("This can happen with no test failures if the RuntimeWarning")
        tr.line("raised by Python when this happens is filtered by a test.")
        tr.line("")
        tr.line("Please ensure all new C modules declare support for running")
        tr.line("without the GIL. Any new tests that intentionally imports ")
        tr.line("code that re-enables the GIL should do so in a subprocess.")
        pytest.exit("GIL re-enabled during tests", returncode=1)

# FIXME when yield tests are gone.
@pytest.hookimpl(tryfirst=True)
def pytest_itemcollected(item):
    """
    Check FPU precision mode was not changed during test collection.

    The clumsy way we do it here is mainly necessary because numpy
    still uses yield tests, which can execute code at test collection
    time.
    """
    global _old_fpu_mode

    mode = get_fpu_mode()

    if _old_fpu_mode is None:
        _old_fpu_mode = mode
    elif mode != _old_fpu_mode:
        _collect_results[item] = (_old_fpu_mode, mode)
        _old_fpu_mode = mode

    # mark f2py tests as thread unsafe
    if Path(item.fspath).parent == Path(__file__).parent / 'f2py' / 'tests':
        item.add_marker(pytest.mark.thread_unsafe(
            reason="f2py tests are thread-unsafe"))


@pytest.fixture(scope="function", autouse=True)
def check_fpu_mode(request):
    """
    Check FPU precision mode was not changed during the test.
    """
    old_mode = get_fpu_mode()
    yield
    new_mode = get_fpu_mode()

    if old_mode != new_mode:
        raise AssertionError(f"FPU precision mode changed from {old_mode:#x} to "
                             f"{new_mode:#x} during the test")

    collect_result = _collect_results.get(request.node)
    if collect_result is not None:
        old_mode, new_mode = collect_result
        raise AssertionError(f"FPU precision mode changed from {old_mode:#x} to "
                             f"{new_mode:#x} when collecting the test")


@pytest.fixture(autouse=True)
def add_np(doctest_namespace):
    doctest_namespace['np'] = numpy


if HAVE_SCPDT:

    @contextmanager
    def warnings_errors_and_rng(test=None):
        """Filter out the wall of DeprecationWarnings.
        """
        msgs = ["The numpy.linalg.linalg",
                "The numpy.fft.helper",
                "dep_util",
                "pkg_resources",
                "numpy.core.umath",
                "msvccompiler",
                "Deprecated call",
                "numpy.core",
                "Importing from numpy.matlib",
                "This function is deprecated.",    # random_integers
                "Arrays of 2-dimensional vectors",   # matlib.cross
                "NumPy warning suppression and assertion utilities are deprecated.",
                "numpy.fix is deprecated",  # fix -> trunc
                "The chararray class is deprecated",  # char.chararray
                "numpy.typename is deprecated",  # typename -> dtype.name
                "numpy.ma.round_ is deprecated",  # ma.round_ -> ma.round
        ]
        msg = "|".join(msgs)

        msgs_r = [
            "invalid value encountered",
            "divide by zero encountered"
        ]
        msg_r = "|".join(msgs_r)

        with warnings.catch_warnings():
            warnings.filterwarnings(
                'ignore', category=DeprecationWarning, message=msg
            )
            warnings.filterwarnings(
                'ignore', category=RuntimeWarning, message=msg_r
            )
            yield

    # find and check doctests under this context manager
    dt_config.user_context_mgr = warnings_errors_and_rng

    # numpy specific tweaks from refguide-check
    dt_config.rndm_markers.add('#uninitialized')
    dt_config.rndm_markers.add('# uninitialized')

    # make the checker pick on mismatched dtypes
    dt_config.strict_check = True

    import doctest
    dt_config.optionflags = doctest.NORMALIZE_WHITESPACE | doctest.ELLIPSIS

--- ./apps/api/pyproject.toml
[project]
name = "beattrack-api"
version = "0.1.0"
description = "Beattrack API — sonically similar song finder"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.0",
    "pydantic>=2.13.4",
    "supabase>=2.31.0",
    "python-magic>=0.4.27",
    "slowapi>=0.1.10",
    "procrastinate[asyncio]>=3.9.0",
    "sse-starlette>=3.4.6",
    "python-multipart>=0.0.20",
    "numpy>=2.5.1",
    "essentia>=2.1b6.dev1110",
    "essentia-tensorflow>=2.1b6.dev1110",
    "pyacoustid>=1.3.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.4.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=5.0.0",
    "httpx>=0.28.0",
    "pip-audit>=2.7.0",
]

[tool.hatch.build.targets.wheel]
packages = ["app"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.2",
    "pytest-asyncio>=0.24.0",
    "ruff>=0.16.0",
    "mypy>=2.3.0",
]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I", "S", "B", "UP", "RUF"]
ignore = ["S101", "S603", "S607"]  # assert in tests, subprocess calls are intentional

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S"]

[tool.mypy]
python_version = "3.12"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "slow: marks tests as slow (skipped unless essentia is installed)",
]
--- ./apps/api/tests/conftest.py
"""Shared pytest fixtures for beattrack API tests."""
import io
import os
import tempfile
from pathlib import Path
from unittest.mock import MagicMock

import pytest
from fastapi.testclient import TestClient

# ---------------------------------------------------------------------------
# Set env vars before any app import so get_supabase() won't crash
# ---------------------------------------------------------------------------
os.environ.setdefault("SUPABASE_URL", "http://mock")
os.environ.setdefault("SUPABASE_ANON_KEY", "mock")

from app.main import app  # noqa: E402 — must be after env vars
from app.db import get_supabase  # noqa: E402


# ---------------------------------------------------------------------------
# Fixture paths
# ---------------------------------------------------------------------------

_FIXTURE_DIR = Path(__file__).parent.parent.parent.parent / "phase0" / "fixtures"


# ---------------------------------------------------------------------------
# Helpers
# ---------------------------------------------------------------------------

def _make_supabase_mock():
    sb = MagicMock()
    builder = MagicMock()
    sb.table.return_value = builder
    for method in ("select", "eq", "ilike", "in_", "range", "insert", "single", "limit"):
        getattr(builder, method).return_value = builder
    return sb, builder


# ---------------------------------------------------------------------------
# Fixtures
# ---------------------------------------------------------------------------

@pytest.fixture()
def client():
    """TestClient with dependency_overrides cleanup."""
    c = TestClient(app)
    yield c
    app.dependency_overrides.clear()


@pytest.fixture()
def supabase_mock():
    """Return (sb, builder) MagicMock tuple."""
    return _make_supabase_mock()


@pytest.fixture()
def audio_wav_path():
    """Absolute path to sine_440hz.wav test fixture."""
    p = _FIXTURE_DIR / "sine_440hz.wav"
    assert p.exists(), f"Fixture not found: {p}"
    return str(p)


@pytest.fixture()
def audio_wav_1000_path():
    """Absolute path to sine_1000hz.wav test fixture."""
    p = _FIXTURE_DIR / "sine_1000hz.wav"
    assert p.exists(), f"Fixture not found: {p}"
    return str(p)


@pytest.fixture()
def fake_mp3_path(tmp_path):
    """Temp file with .mp3 extension but PHP content (MIME mismatch test)."""
    f = tmp_path / "fake.mp3"
    f.write_bytes(b"<?php echo 'evil'; ?>")
    return str(f)


@pytest.fixture()
def oversized_file_path(tmp_path):
    """Sparse temp file >50 MB (does not actually allocate 50 MB on disk)."""
    f = tmp_path / "big.mp3"
    # Create a sparse file: seek past 50 MB + 1 byte and write a single null byte
    fifty_mb_plus_one = 50 * 1024 * 1024 + 1
    with open(f, "wb") as fh:
        fh.seek(fifty_mb_plus_one)
        fh.write(b"\x00")
    return str(f)
--- ./phase0/pyproject.toml
[project]
name = "beattrack-eval"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "essentia-tensorflow>=2.1b6.dev1389",
    "numpy>=2.4.3",
    "pandas>=2.2.0",
]

exec
/bin/zsh -lc "git log --oneline --decorate -8 -- apps/api/app/services/jobs.py apps/api/app/routes/analyze.py apps/api/tests/test_analyze.py; printf '%s\\n' '--- route test references ---'; rg -n \"analyze|update_job_status|get_job|analysis_jobs\" apps/api/tests apps/api/app | head -300; printf '%s\\n' '--- test command ---'; cd apps/api && PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest tests/test_analyze.py -p no:cacheprovider" in /Users/ten.december/claude-projects/beattrack
 exited 1 in 2104ms:
2026-07-31 08:39:38.553 xcodebuild[83274:2567915]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:38.727 xcodebuild[83274:2567914] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-rZcHYAoJ' (errno=Operation not permitted)
2026-07-31 08:39:39.177 xcodebuild[83276:2567926]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:39.304 xcodebuild[83276:2567925] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
1ba55c5 Repair analyze tests after job status moved to Postgres
6061057 Audio-Analyse repariert: Job-Status in analysis_jobs statt Prozessspeicher
a7a59d6 fix: comprehensive security hardening across API, DB, frontend, and CI
cea87ef feat: Phase 2 tests, rate limiting, and worker DB integration
3a61a34 feat: add audio upload pipeline (Phase 2)
--- route test references ---
apps/api/app/routes/analyze.py:21:router = APIRouter(prefix="/analyze", tags=["analyze"])
apps/api/app/routes/analyze.py:42:async def upload_and_analyze(request: Request, file: UploadFile):
apps/api/app/routes/analyze.py:80:            from app.workers import app as procrastinate_app, analyze_audio
apps/api/app/routes/analyze.py:81:            analyze_audio.defer(audio_path=temp_path, job_id=job_id)
apps/api/app/routes/analyze.py:84:            await asyncio.to_thread(jobs.update_job_status, job_id, "failed", error="enqueue failed")
apps/api/app/routes/analyze.py:101:    if await asyncio.to_thread(jobs.get_job, job_id) is None:
apps/api/app/routes/analyze.py:116:                job = await asyncio.to_thread(jobs.get_job, job_id)
apps/api/app/routes/analyze.py:172:    job = await asyncio.to_thread(jobs.get_job, job_id)
apps/api/app/workers/__init__.py:155:@app.task(name="analyze_audio", retry=1, pass_context=True)
apps/api/app/workers/__init__.py:156:def analyze_audio(context, *, audio_path: str, job_id: str):
apps/api/app/workers/__init__.py:165:    from app.services.jobs import update_job_status
apps/api/app/workers/__init__.py:167:    update_job_status(job_id, "processing", progress=0.1)
apps/api/app/workers/__init__.py:174:        update_job_status(job_id, "failed", error=str(exc))
apps/api/app/workers/__init__.py:180:    update_job_status(job_id, "processing", progress=0.8)
apps/api/app/workers/__init__.py:196:    update_job_status(job_id, "completed", progress=1.0, result=result)
apps/api/app/main.py:15:from app.routes import songs, similar, feedback, analyze, identify, batch_ingest
apps/api/app/main.py:55:    from app.routes.analyze import TEMP_DIR
apps/api/app/main.py:94:app.include_router(analyze.router)
apps/api/tests/test_analyze.py:1:"""Tests for app/routes/analyze.py."""
apps/api/tests/test_analyze.py:26:    """POST /analyze without a file should return 422 (validation error)."""
apps/api/tests/test_analyze.py:27:    resp = client.post("/analyze")
apps/api/tests/test_analyze.py:35:# these routes hit the database. get_job is patched to keep tests offline.
apps/api/tests/test_analyze.py:39:    """GET /analyze/<unknown>/results should return 404."""
apps/api/tests/test_analyze.py:40:    with patch("app.services.jobs.get_job", return_value=None):
apps/api/tests/test_analyze.py:41:        resp = client.get("/analyze/unknown-job-id/results")
apps/api/tests/test_analyze.py:46:    """GET /analyze/<unknown>/stream should return 404."""
apps/api/tests/test_analyze.py:47:    with patch("app.services.jobs.get_job", return_value=None):
apps/api/tests/test_analyze.py:48:        resp = client.get("/analyze/unknown-job-id/stream")
apps/api/tests/test_analyze.py:57:    """POST /analyze with a .txt file should be rejected with 400."""
apps/api/tests/test_analyze.py:60:        "/analyze",
apps/api/tests/test_analyze.py:67:# Tests — jobs.update_job_status
apps/api/tests/test_analyze.py:70:def test_update_job_status_writes_progress():
apps/api/tests/test_analyze.py:71:    """update_job_status() should write status and progress to analysis_jobs."""
apps/api/tests/test_analyze.py:74:        jobs.update_job_status("test-job-status-001", "processing", progress=0.5)
apps/api/tests/test_analyze.py:82:def test_update_job_status_completed_carries_result():
apps/api/tests/test_analyze.py:86:        jobs.update_job_status(
apps/api/tests/test_analyze.py:96:def test_update_job_status_survives_db_error():
apps/api/tests/test_analyze.py:101:        jobs.update_job_status("nonexistent-id", "processing")
apps/api/tests/test_analyze.py:105:# Tests — jobs.get_job
apps/api/tests/test_analyze.py:108:def test_get_job_unknown_returns_none():
apps/api/tests/test_analyze.py:113:        assert jobs.get_job("nonexistent-id") is None
apps/api/tests/test_analyze.py:116:def test_get_job_reports_stalled_job_as_failed():
apps/api/tests/test_analyze.py:131:        job = jobs.get_job("stalled-job")
apps/api/app/services/jobs.py:1:"""Job-Status in Postgres (Tabelle analysis_jobs) statt im Prozessspeicher.
apps/api/app/services/jobs.py:5:unsichtbar. Die Tabelle analysis_jobs existierte bereits mit passendem Schema
apps/api/app/services/jobs.py:34:    get_supabase().table("analysis_jobs").insert({
apps/api/app/services/jobs.py:43:def update_job_status(job_id: str, status: str, progress: float | None = None,
apps/api/app/services/jobs.py:56:        get_supabase().table("analysis_jobs").update(row).eq("id", job_id).execute()
apps/api/app/services/jobs.py:61:def get_job(job_id: str) -> dict | None:
apps/api/app/services/jobs.py:68:    res = get_supabase().table("analysis_jobs").select("*").eq("id", job_id).execute()
apps/api/app/services/jobs.py:114:        res = get_supabase().table("analysis_jobs").select("id,status").in_("id", ids).execute()
--- test command ---
Traceback (most recent call last):
  File "<frozen runpy>", line 198, in _run_module_as_main
  File "<frozen runpy>", line 88, in _run_code
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pytest/__main__.py", line 9, in <module>
    raise SystemExit(_console_main())
                     ^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/config/__init__.py", line 253, in _console_main
    code = _main(prog=_get_prog_name(sys.argv))
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/config/__init__.py", line 223, in _main
    config = _prepareconfig(new_args, plugins, prog=prog)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/config/__init__.py", line 410, in _prepareconfig
    config: Config = pluginmanager.hook.pytest_cmdline_parse(
                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/helpconfig.py", line 124, in pytest_cmdline_parse
    config = yield
             ^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_callers.py", line 121, in _multicall
    res = hook_impl.function(*args)
          ^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/config/__init__.py", line 1232, in pytest_cmdline_parse
    self.parse(args)
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/config/__init__.py", line 1605, in parse
    self.hook.pytest_load_initial_conftests(
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_hooks.py", line 512, in __call__
    return self._hookexec(self.name, self._hookimpls.copy(), kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_manager.py", line 120, in _hookexec
    return self._inner_hookexec(hook_name, methods, kwargs, firstresult)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_callers.py", line 167, in _multicall
    raise exception
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_callers.py", line 139, in _multicall
    teardown.throw(exception)
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/warnings.py", line 129, in pytest_load_initial_conftests
    return (yield)
            ^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/pluggy/_callers.py", line 116, in _multicall
    next(function_gen)  # first yield
    ^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/capture.py", line 170, in pytest_load_initial_conftests
    capman.start_global_capturing()
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/capture.py", line 773, in start_global_capturing
    self._global_capturing = _get_multicapture(self._method)
                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/capture.py", line 714, in _get_multicapture
    return MultiCapture(in_=FDCapture(0), out=FDCapture(1), err=FDCapture(2))
                                              ^^^^^^^^^^^^
  File "/Users/ten.december/claude-projects/beattrack/apps/api/.venv/lib/python3.12/site-packages/_pytest/capture.py", line 493, in __init__
    TemporaryFile(buffering=0),
    ^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/tempfile.py", line 631, in TemporaryFile
    prefix, suffix, dir, output_type = _sanitize_params(prefix, suffix, dir)
                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/tempfile.py", line 126, in _sanitize_params
    dir = gettempdir()
          ^^^^^^^^^^^^
  File "/Users/ten.december/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/tempfile.py", line 315, in gettempdir
    return _os.fsdecode(_gettempdir())
                        ^^^^^^^^^^^^^
  File "/Users/ten.december/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/tempfile.py", line 308, in _gettempdir
    tempdir = _get_default_tempdir()
              ^^^^^^^^^^^^^^^^^^^^^^
  File "/Users/ten.december/.local/share/uv/python/cpython-3.12.13-macos-aarch64-none/lib/python3.12/tempfile.py", line 223, in _get_default_tempdir
    raise FileNotFoundError(_errno.ENOENT,
FileNotFoundError: [Errno 2] No usable temporary directory found in ['/var/folders/dc/n2hbm_491614rmzw6lw6s2n80000gn/T/', '/tmp', '/var/tmp', '/usr/tmp', '/Users/ten.december/claude-projects/beattrack/apps/api']

exec
/bin/zsh -lc "git show --format=fuller --no-ext-diff --unified=80 6061057 -- apps/api/app/services/jobs.py apps/api/app/routes/analyze.py apps/api/tests/test_analyze.py; printf '%s\\n' '--- all test files ---'; find apps/api/tests -type f -maxdepth 2 -not -path '*/__pycache__/*' -print | sort" in /Users/ten.december/claude-projects/beattrack
 succeeded in 1628ms:
2026-07-31 08:39:45.694 xcodebuild[83287:2568063]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:45.815 xcodebuild[83287:2568061] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-jAskeTPQ' (errno=Operation not permitted)
2026-07-31 08:39:46.272 xcodebuild[83289:2568086]  DVTFilePathFSEvents: Failed to start fs event stream.
2026-07-31 08:39:46.384 xcodebuild[83289:2568085] [MT] DVTDeveloperPaths: Failed to get length of DARWIN_USER_CACHE_DIR from confstr(3), error = Error Domain=NSPOSIXErrorDomain Code=5 "Input/output error". Using NSCachesDirectory instead.
commit 606105789173d2c469be9c874acf50f43bef101d
Author:     Basti <dm-basti@pm.me>
AuthorDate: Thu Jul 30 15:08:22 2026 +0200
Commit:     Basti <dm-basti@pm.me>
CommitDate: Thu Jul 30 15:08:22 2026 +0200

    Audio-Analyse repariert: Job-Status in analysis_jobs statt Prozessspeicher
    
    Umsetzung von infra-migration/docs/plan-audio-analyse.md (v3):
    - services/jobs.py: create/update/get + job-bewusstes Aufraeumen (24h-Orphans, 5GB-Cap)
    - analyze.py: DB-Zugriffe via asyncio.to_thread, SSE-Timeout 300->900s,
      Datei-Loeschung im Enqueue-Fehlerpfad
    - Haengend-Erkennung: processing ohne Update >5min -> failed (nur Anzeige)
    - workers: update_job_status direkt aus services.jobs
    - main.py: rmtree beim Shutdown/atexit entfernt (gemeinsames Volume!)
    - Dockerfile: Schema-Fehler bricht Worker-Start ab (|| true entfernt)
    
    --no-verify: lokaler Lint kaputt (bekanntes Gotcha), CI prueft.
    
    Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

diff --git a/apps/api/app/routes/analyze.py b/apps/api/app/routes/analyze.py
index be37a3a..3a06634 100644
--- a/apps/api/app/routes/analyze.py
+++ b/apps/api/app/routes/analyze.py
@@ -1,205 +1,183 @@
 """Audio analysis endpoints — upload, SSE streaming, results polling."""
 import asyncio
 import json
 import logging
 import os
 import tempfile
-import time
 import uuid
 from pathlib import Path
 
 from fastapi import APIRouter, HTTPException, Request, UploadFile
 from fastapi.responses import JSONResponse
 from pydantic import BaseModel
 from sse_starlette.sse import EventSourceResponse
 
 from app.limiter import limiter
+from app.services import jobs
 from app.services.validation import validate_upload, validate_audio
 
 logger = logging.getLogger(__name__)
 
 router = APIRouter(prefix="/analyze", tags=["analyze"])
 
 # Concurrency controls
 UPLOAD_SEMAPHORE = asyncio.Semaphore(3)  # Max 3 concurrent uploads
 SSE_LIMITER = asyncio.Semaphore(50)  # Max 50 SSE connections
 
 # Temp directory for uploads
 TEMP_DIR = os.environ.get("BEATTRACK_TEMP_DIR", tempfile.mkdtemp(prefix="beattrack_"))
 Path(TEMP_DIR).mkdir(parents=True, exist_ok=True)
 
-# In-memory job status tracking (simple dict, sufficient for single-worker)
-# In production, this would be read from procrastinate_jobs table
-_job_status: dict[str, dict] = {}
-_JOB_TTL_SEC = 3600  # Remove completed/failed jobs after 1 hour
-
-
-def _cleanup_stale_jobs() -> None:
-    """Remove jobs older than _JOB_TTL_SEC from memory."""
-    now = time.time()
-    stale = [
-        jid for jid, job in _job_status.items()
-        if job.get("status") in ("completed", "failed")
-        and now - job.get("created_at", now) > _JOB_TTL_SEC
-    ]
-    for jid in stale:
-        del _job_status[jid]
+# Job-Zustand liegt in Postgres (app.services.jobs) — API und Worker sind
+# getrennte Container und teilen keinen Prozessspeicher mehr.
 
 
 class AnalyzeResponse(BaseModel):
     job_id: str
     status: str
 
 
 @router.post("", response_model=AnalyzeResponse)
 @limiter.limit("10/minute")
 async def upload_and_analyze(request: Request, file: UploadFile):
     """Upload audio file for analysis.
 
     Validates the file, saves to temp, enqueues analysis job.
     Returns job_id for tracking via SSE or polling.
     """
     # Check capacity
     if UPLOAD_SEMAPHORE._value == 0:
         raise HTTPException(status_code=503, detail="Server at capacity. Please retry shortly.")
 
     async with UPLOAD_SEMAPHORE:
         # 1. Validate upload (MIME + size)
         await validate_upload(file)
 
         # 2. Save to temp file
         job_id = str(uuid.uuid4())
         ext = Path(file.filename or "audio.mp3").suffix or ".mp3"
         temp_path = os.path.join(TEMP_DIR, f"{job_id}{ext}")
 
         try:
             content = await file.read()
             with open(temp_path, "wb") as f:
                 f.write(content)
         except Exception as exc:
             logger.error("Failed to save upload: %s", exc)
             raise HTTPException(status_code=500, detail="Failed to save uploaded file.")
 
         # 3. Validate audio (ffprobe: format, duration) — sync call, run in executor
         loop = asyncio.get_event_loop()
         audio_info = await loop.run_in_executor(None, validate_audio, temp_path)
 
         # 4. Track job status
-        _cleanup_stale_jobs()
-        _job_status[job_id] = {
-            "status": "queued",
-            "progress": 0.0,
-            "audio_path": temp_path,
-            "duration_sec": audio_info.get("duration_sec"),
-            "created_at": time.time(),
-        }
+        await asyncio.to_thread(
+            jobs.create_job, job_id, temp_path, audio_info.get("duration_sec")
+        )
 
         # 5. Enqueue analysis job
         try:
             from app.workers import app as procrastinate_app, analyze_audio
             analyze_audio.defer(audio_path=temp_path, job_id=job_id)
         except Exception as exc:
             logger.error("Failed to enqueue job %s: %s", job_id, exc)
-            _job_status[job_id]["status"] = "failed"
+            await asyncio.to_thread(jobs.update_job_status, job_id, "failed", error="enqueue failed")
+            Path(temp_path).unlink(missing_ok=True)
             raise HTTPException(status_code=502, detail="Failed to start analysis.")
 
         return AnalyzeResponse(job_id=job_id, status="queued")
 
 
 @router.get("/{job_id}/stream")
 async def stream_progress(job_id: str):
     """SSE endpoint for real-time analysis progress.
 
     Events:
     - status: queued | processing | completed | failed
     - progress: 0.0 - 1.0 (during processing)
     - result: full analysis result (on completed)
     - heartbeat: keepalive every 15s
     """
-    if job_id not in _job_status:
+    if await asyncio.to_thread(jobs.get_job, job_id) is None:
         raise HTTPException(status_code=404, detail="Job not found")
 
     if SSE_LIMITER._value == 0:
         raise HTTPException(status_code=503, detail="Too many active connections.")
 
     async def event_generator():
         async with SSE_LIMITER:
             heartbeat_interval = 15
-            stale_timeout = 300
+            # 900 s statt 300: abgestimmt auf die Hängend-Erkennung (300 s) in
+            # services.jobs — eine langsame echte Analyse wird nicht abgeschnitten.
+            stale_timeout = 900
             elapsed = 0.0
 
             while elapsed < stale_timeout:
-                job = _job_status.get(job_id)
+                job = await asyncio.to_thread(jobs.get_job, job_id)
                 if not job:
                     yield {"event": "error", "data": json.dumps({"detail": "Job not found"})}
                     return
 
                 status = job["status"]
 
                 if status == "completed":
                     yield {
                         "event": "status",
                         "data": json.dumps({
                             "status": "completed",
                             "progress": 1.0,
                             "result": job.get("result"),
                         }),
                     }
                     return
 
                 if status == "failed":
                     yield {
                         "event": "status",
                         "data": json.dumps({
                             "status": "failed",
                             "error": job.get("error", "Analysis failed"),
                         }),
                     }
                     return
 
                 # Send current status
                 yield {
                     "event": "status",
                     "data": json.dumps({
                         "status": status,
                         "progress": job.get("progress", 0.0),
                     }),
                 }
 
                 # Wait before next update
                 await asyncio.sleep(heartbeat_interval)
                 elapsed += heartbeat_interval
 
                 # Send heartbeat
                 yield {"event": "heartbeat", "data": ""}
 
             # Stale timeout reached
             yield {"event": "error", "data": json.dumps({"detail": "Connection timed out"})}
 
     return EventSourceResponse(event_generator())
 
 
 @router.get("/{job_id}/results")
 async def get_results(job_id: str):
     """Polling fallback for analysis results.
 
     Returns current job status. If completed, includes full results.
     """
-    job = _job_status.get(job_id)
+    job = await asyncio.to_thread(jobs.get_job, job_id)
     if not job:
         raise HTTPException(status_code=404, detail="Job not found")
 
     return JSONResponse(content={
         "job_id": job_id,
         "status": job["status"],
         "progress": job.get("progress", 0.0),
         "result": job.get("result") if job["status"] == "completed" else None,
         "error": job.get("error") if job["status"] == "failed" else None,
     })
 
-
-def update_job_status(job_id: str, status: str, **kwargs):
-    """Update job status from worker. Called by the analyze task."""
-    if job_id in _job_status:
-        _job_status[job_id]["status"] = status
-        _job_status[job_id].update(kwargs)
diff --git a/apps/api/app/services/jobs.py b/apps/api/app/services/jobs.py
new file mode 100644
index 0000000..f3f86ee
--- /dev/null
+++ b/apps/api/app/services/jobs.py
@@ -0,0 +1,140 @@
+"""Job-Status in Postgres (Tabelle analysis_jobs) statt im Prozessspeicher.
+
+API und Worker laufen als getrennte Container. Ein Dictionary im Speicher ist
+dann pro Prozess privat — der Fortschritt des Workers wäre für die API
+unsichtbar. Die Tabelle analysis_jobs existierte bereits mit passendem Schema
+und wird hier erstmals verdrahtet (siehe infra-migration/docs/plan-audio-analyse.md).
+
+Alle Funktionen sind synchron (der Supabase-Client blockiert). In asynchronen
+Pfaden immer über asyncio.to_thread aufrufen.
+"""
+import logging
+from datetime import datetime, timezone
+from pathlib import Path
+
+from app.db import get_supabase
+
+logger = logging.getLogger(__name__)
+
+# Ein processing-Job ohne Statusupdate seit dieser Zeit gilt als abgestürzt.
+# Der Worker meldet Fortschritt bei 0.1/0.8/1.0 — Stillstand heißt Absturz.
+# Bewusst unterhalb des SSE-Abbruchs (900 s), damit der Nutzer die Meldung sieht.
+STALL_AFTER_SEC = 300
+
+# Notbremsen für den Upload-Ordner (gemeinsames Volume, wird nie pauschal geleert)
+ORPHAN_MAX_AGE_SEC = 24 * 3600
+VOLUME_CAP_BYTES = 5 * 1024**3
+
+
+def _now() -> str:
+    return datetime.now(timezone.utc).isoformat()
+
+
+def create_job(job_id: str, audio_path: str, duration_sec: float | None) -> None:
+    get_supabase().table("analysis_jobs").insert({
+        "id": job_id,
+        "status": "queued",
+        "progress": 0.0,
+        "audio_path": audio_path,
+        "duration_sec": duration_sec,
+    }).execute()
+
+
+def update_job_status(job_id: str, status: str, progress: float | None = None,
+                      result: dict | None = None, error: str | None = None) -> None:
+    """Statusupdate aus API oder Worker. Signatur kompatibel zum alten Wrapper."""
+    row: dict = {"status": status, "updated_at": _now()}
+    if progress is not None:
+        row["progress"] = progress
+    if result is not None:
+        row["result"] = result
+    if error is not None:
+        row["last_error"] = error
+    if status in ("completed", "failed"):
+        row["completed_at"] = _now()
+    try:
+        get_supabase().table("analysis_jobs").update(row).eq("id", job_id).execute()
+    except Exception as exc:  # Status-Updates dürfen die Analyse nie abbrechen
+        logger.warning("Job-Statusupdate fehlgeschlagen (%s -> %s): %s", job_id, status, exc)
+
+
+def get_job(job_id: str) -> dict | None:
+    """Liefert den Job im Format des alten _job_status-Eintrags, oder None.
+
+    Wendet die Hängend-Regel an: processing ohne Update seit STALL_AFTER_SEC
+    wird als failed gemeldet (nur in der Antwort — die Zeile bleibt unverändert,
+    ein erfolgreicher später eintreffender Worker-Update gewinnt).
+    """
+    res = get_supabase().table("analysis_jobs").select("*").eq("id", job_id).execute()
+    if not res.data:
+        return None
+    row = res.data[0]
+    status = row["status"]
+
+    if status == "processing" and row.get("updated_at"):
+        updated = datetime.fromisoformat(row["updated_at"].replace("Z", "+00:00"))
+        age = (datetime.now(timezone.utc) - updated).total_seconds()
+        if age > STALL_AFTER_SEC:
+            return {
+                "status": "failed",
+                "progress": row.get("progress", 0.0),
+                "error": "Analysis stalled (no progress for 5 minutes)",
+                "audio_path": row.get("audio_path"),
+            }
+
+    return {
+        "status": status,
+        "progress": row.get("progress", 0.0),
+        "result": row.get("result"),
+        "error": row.get("last_error"),
+        "audio_path": row.get("audio_path"),
+        "duration_sec": row.get("duration_sec"),
+    }
+
+
+def cleanup_temp_files(temp_dir: str) -> None:
+    """Räumt den gemeinsamen Upload-Ordner auf, ohne laufende Jobs zu gefährden.
+
+    Löscht nur: Dateien erledigter Jobs (completed/failed), verwaiste Dateien
+    ohne Job-Zeile älter als 24 h, und — als harte Obergrenze — die ältesten
+    Dateien ohne aktiven Job, bis das Volumen wieder unter 5 GB liegt.
+    Ein pauschales Leeren (früher rmtree beim Shutdown) gibt es bewusst nicht mehr.
+    """
+    root = Path(temp_dir)
+    if not root.exists():
+        return
+    files = [f for f in root.iterdir() if f.is_file()]
+    if not files:
+        return
+
+    ids = [f.stem for f in files]
+    active: set[str] = set()
+    finished: set[str] = set()
+    try:
+        res = get_supabase().table("analysis_jobs").select("id,status").in_("id", ids).execute()
+        for row in res.data or []:
+            (finished if row["status"] in ("completed", "failed") else active).add(str(row["id"]))
+    except Exception as exc:
+        logger.warning("Cleanup: Job-Abfrage fehlgeschlagen, lösche nur nach Alter: %s", exc)
+
+    import time
+    now = time.time()
+    for f in files:
+        try:
+            if f.stem in finished:
+                f.unlink(missing_ok=True)
+            elif f.stem not in active and now - f.stat().st_mtime > ORPHAN_MAX_AGE_SEC:
+                f.unlink(missing_ok=True)
+        except OSError as exc:
+            logger.warning("Cleanup: %s nicht löschbar: %s", f.name, exc)
+
+    remaining = sorted((f for f in root.iterdir() if f.is_file()), key=lambda f: f.stat().st_mtime)
+    total = sum(f.stat().st_size for f in remaining)
+    for f in remaining:
+        if total <= VOLUME_CAP_BYTES:
+            break
+        if f.stem in active:
+            continue
+        total -= f.stat().st_size
+        f.unlink(missing_ok=True)
+        logger.warning("Cleanup: Volumen-Obergrenze — %s gelöscht", f.name)
--- all test files ---
apps/api/tests/__init__.py
apps/api/tests/conftest.py
apps/api/tests/test_acoustid.py
apps/api/tests/test_analyze.py
apps/api/tests/test_apple_music.py
apps/api/tests/test_cleanup.py
apps/api/tests/test_features.py
apps/api/tests/test_feedback_stats.py
apps/api/tests/test_health.py
apps/api/tests/test_http_safe.py
apps/api/tests/test_musicbrainz.py
apps/api/tests/test_routes.py
apps/api/tests/test_similarity_logic.py
apps/api/tests/test_songs_features.py
apps/api/tests/test_soundcloud.py
apps/api/tests/test_spotify.py
apps/api/tests/test_validation.py
apps/api/tests/test_youtube.py

codex
Die bisherigen Verhaltensprüfungen wurden weitgehend sinnvoll auf Postgres angepasst. Zwei neue Mocks prüfen jedoch nicht, ob die Datenbankoperation wirklich ausgeführt und auf die richtige Tabelle sowie Job-ID begrenzt wird. Der Testlauf konnte in der schreibgeschützten Umgebung nicht starten, weil kein temporäres Verzeichnis verfügbar ist.

Full review comments:

- [P2] Assert that the status update is actually persisted — /Users/ten.december/claude-projects/beattrack/apps/api/tests/test_analyze.py:76-79
  Wenn ein späterer Fehler `.execute()` entfernt oder auf eine falsche Tabelle schreibt, bleibt dieser Test grün: Er prüft nur den vorbereiteten Update-Inhalt. Dann würden Worker-Statusänderungen nie in Postgres gespeichert, obwohl die Tests bestehen. Prüfe zusätzlich den Aufruf von `table("analysis_jobs")` und `execute()`.

- [P2] Assert that job lookups are scoped to the requested ID — /Users/ten.december/claude-projects/beattrack/apps/api/tests/test_analyze.py:108-113
  Wenn der `eq("id", job_id)`-Filter später entfällt oder sich ändert, liefert `get_job` bei vorhandenen Daten möglicherweise den Job eines anderen Nutzers. Dieser Test bleibt trotzdem grün, weil sein Mock unabhängig vom angefragten Job immer eine leere Antwort liefert. Prüfe den Tabellen- und `eq`-Aufruf explizit.

